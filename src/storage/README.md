# Storage Module

Planned components: `Database`, `Value`, `HashTable`, `List`, `SkipList`, `ZSet`, and
`ExpireIndex`.

The first end-to-end version may use standard containers before custom data structures replace
them behind stable interfaces.
