# Scripts

- `test-network.sh`: builds and runs all phase-one network tests with strict warnings, then
  repeats them with ASan and UBSan.

Fault-injection and benchmark automation will be added as those modules become executable.
